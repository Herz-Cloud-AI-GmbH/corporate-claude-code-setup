# ============================================================================
# CORPORATE CLAUDE CODE SETUP - ENVIRONMENT CONFIGURATION
# ============================================================================
# This file contains ALL environment variables needed for the multi-provider
# setup. Copy this to `.env` and configure for your chosen provider.
#
# Quick start:
#   cp .devcontainer/sample.env .devcontainer/.env
#   # Edit .devcontainer/.env with your values
#   make setup-<gcp|ollama|copilot>
#
# ============================================================================

# ============================================================================
# LITELLM PROXY CONFIGURATION (REQUIRED FOR ALL PROVIDERS)
# ============================================================================

# LiteLLM config file to use (determines which provider)
# Options: config.gcp.yaml | config.ollama.yaml | config.copilot.yaml
LITELLM_CONFIG=config.gcp.yaml

# LiteLLM proxy authentication key
# Generate a random key: openssl rand -hex 32
LITELLM_MASTER_KEY=sk-CHANGE-THIS-TO-RANDOM-UUID

# LiteLLM proxy port (default: 4000)
LITELLM_PORT=4000

# ============================================================================
# PROFILE IDENTIFIER (REQUIRED)
# ============================================================================
# Identifies which provider configuration is active
# Options: gcp | ollama | copilot
PROFILE=gcp

# ============================================================================
# GCP VERTEX AI CONFIGURATION (REQUIRED ONLY IF PROFILE=gcp)
# ============================================================================

# Your Google Cloud Project ID where Vertex AI is enabled
GCP_PROJECT_ID=your-gcp-project-id

# Vertex AI region (typically us-central1 or global)
CLOUD_ML_REGION=us-central1

# ============================================================================
# OLLAMA CONFIGURATION (REQUIRED ONLY IF PROFILE=ollama)
# ============================================================================
# No environment variables needed for Ollama.
#
# IMPORTANT SETUP STEPS:
# 1. Ollama must run on your HOST machine (not in the devcontainer)
# 2. The devcontainer connects via: host.docker.internal:11434
# 3. Before running 'make setup-ollama':
#    - Open terminal on HOST (outside devcontainer)
#    - Start Ollama: ollama serve
#    - Pull models: ollama pull llama3.1:70b llama3.1:8b qwen2.5:3b
#
# See: https://ollama.ai/library for available models

# ============================================================================
# GITHUB COPILOT CONFIGURATION (REQUIRED ONLY IF PROFILE=copilot)
# ============================================================================
# No environment variables needed for GitHub Copilot.
#
# IMPORTANT SETUP STEPS:
# 1. Ensure you have an active GitHub Copilot subscription
# 2. Log in to GitHub Copilot in VS Code/Cursor
# 3. Check VS Code status bar to verify Copilot is active
# 4. Run: make setup-copilot

# ============================================================================
# CLAUDE CODE CONFIGURATION (INFORMATIONAL ONLY - NOT USED IN .env)
# ============================================================================
# Claude Code settings are configured in ~/.claude/settings.json
# The Makefile automatically generates this file with the following values:
#
# ANTHROPIC_BASE_URL=http://localhost:4000 (LiteLLM proxy)
# ANTHROPIC_AUTH_TOKEN=<same as LITELLM_MASTER_KEY>
# ANTHROPIC_MODEL=sonnet
# ANTHROPIC_DEFAULT_OPUS_MODEL=opus
# ANTHROPIC_DEFAULT_SONNET_MODEL=sonnet
# ANTHROPIC_DEFAULT_HAIKU_MODEL=haiku
# CLAUDE_CODE_SUBAGENT_MODEL=sonnet
#
# These values are NOT set in this .env file.
# They are managed by the Makefile during setup.

# ============================================================================
# DEPRECATED / REMOVED VARIABLES
# ============================================================================
# The following variables were used in older versions and are NO LONGER NEEDED:
#
# CLAUDE_CODE_USE_VERTEX - Removed (LiteLLM handles provider routing)
# ANTHROPIC_VERTEX_PROJECT_ID - Removed (use GCP_PROJECT_ID instead)
# ANTHROPIC_MODEL - Removed (configured in ~/.claude/settings.json)
# ANTHROPIC_SMALL_FAST_MODEL - Removed (configured in ~/.claude/settings.json)
# DEVCONTAINER - Removed (not needed)
# CLAUDE_CODE_VERSION - Removed (set in devcontainer.json build args)
#
# ============================================================================
